<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <% if (locals.games){ %>
        <% for (const game of games){ %>
            <% const {gameID, gameName, gameDescription, ...categories} = game;%>
            <%- include("partials/game", {gameID: gameID, gameName: gameName, gameDescription: gameDescription, categories: categories})  %>
        <% }} %>
    </div>
</body>
</html>