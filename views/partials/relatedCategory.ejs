<div>
    <% if(Object.keys(categories) !== 0){ %>
        <%=name.toUpperCase() %>
    <% }; %> 
    <% for (category of categories) {%>
        <div>
        <% if (category) { %>
        <%= category.name %>
        <% } %>
        </div>
    <% }%>
</div>