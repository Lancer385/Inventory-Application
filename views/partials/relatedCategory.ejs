<div>
    <% if(categories.platforms){ %>
        <%=name.toUpperCase() %>
    <% }; %> 
    <% for (category of categories) {%>
        <div>
        <% if (category) { %>
        <%= category.name %>
        <% } %>
        </div>
    <% }%>
</div>